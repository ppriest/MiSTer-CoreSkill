# DIP switches and inputs

Survey of the KonamiGX core, the JalecoMS32 core, the Seta core, the Fuuki core, the Psikyo core (top-level trees; `build/` copies ignored).

## 1. Summary

Reference implementation: **Seta** for the pipeline (extract_dips.py parser + build_mra.py `dip_xml`/`buttons_xml` + check_dips.py/check_inputs.py against MAME `-listxml` and a Lua port dump). **KonamiGX** is the latest copy of the same parser (its header says "From Arcade-Seta_MiSTer") and adds the OSD 28-column fit check; **MS32** is the same parser again ("Copied from Arcade-Seta_MiSTer"). Fuuki has a hand-typed DIP table (the thing extract_dips.py was written to replace). Psikyo has no DIP generator at all; its .mra switches are hand-written and only validated structurally.

The core-side wiring is the same in all five: `<switches>` bytes arrive on ioctl index 254, one byte per address, latched into a `sw[]`/`dip_sw` register; joystick bits come from `hps_io` `joystick_0..3` with bit 4+i = i-th name in the .mra `<buttons>` list.

## 2. Per-core table

| Core | DIP source | .mra emitter | Core-side latch | Check tool | Players |
|---|---|---|---|---|---|
| KonamiGX | `scripts/extract_dips.py` (parses konamigx.cpp INPUT_PORTS) | `scripts/build_mra.py:249-292 dips_of()`, `:391-395` | `KonamiGX.sv:222-228` `sw[2]` | none (OSD width assert only) | 2 |
| MS32 | `scripts/extract_dips.py` (ms32.cpp) | `scripts/build_mra.py:114-138 switches_xml()` | `MS32.sv:220-224` `sw[4]` | none | 2 + PS/2 mahjong matrix |
| Seta | `scripts/extract_dips.py` (seta.cpp) | `scripts/build_mra.py:639-686 dip_xml()`, `:809-833 buttons_xml()`, `:1057-1084` | `Seta.sv:288-295` `sw[8]` | `scripts/check_dips.py`, `scripts/check_inputs.py` + `scripts/mame/ports.lua` | 4 (wits), guns via mouse, rotary (Downtown) |
| Fuuki | hand table `scripts/build_mra.py:104-215` | `scripts/build_mra.py:484-518 dip_xml()`, `:559-566 buttons_xml()`, `:640-648` | `Fuuki.sv:371-381` `dip_sw[63:0]` | none | 2 |
| Psikyo | hand-written in the .mra | none (`scripts/deploy_mra.py` copies; `scripts/validate_mra.py:87-97` checks byte count vs bits) | `Psikyo.sv:436-454` `dip_sw[63:0]` | validate_mra.py (structure only) | 2 |

All five: `hps_io #(.CONF_STR(CONF_STR))` with `"DIP;"` in CONF_STR (KonamiGX.sv:66, MS32.sv:99, Seta.sv:119, Fuuki.sv:117, Psikyo.sv:134). The OSD DIP page is generated by the MiSTer main from the .mra `<switches>`, not from CONF_STR.

## 3. Pipeline

### 3a. MAME driver -> extract_dips.py

Not `-listxml`: the parser reads the driver's C++ source. the Seta core's `scripts/extract_dips.py:1-38` (identical prose in KonamiGX and MS32 copies):

```
python scripts/extract_dips.py thunderl
python scripts/extract_dips.py --all           # every Group A game
python scripts/extract_dips.py --selftest
```
`SRC = os.getenv("MAME_SRC", "$MAME_SRC/src/mame/seta/seta.cpp")` (Seta :46; KonamiGX :46 `konami/konamigx.cpp`; MS32 :46 `jaleco/ms32.cpp`).

What is parsed (from the docstring): `PORT_DIPNAME`, `PORT_DIPSETTING`, `PORT_DIPUNUSED_DIPLOC` (default kept, no `<dip>` emitted), `PORT_SERVICE_DIPLOC`, `PORT_BIT` ignored; `PORT_INCLUDE` followed (`parse_ports(body, all_blocks, missing, depth)` Seta :131). `DEF_STR` table is local and `--selftest` fails on a missing token. Seta `_fold()` :267 folds two switches whose meaning depends on each other into one "(a|b)" entry.

Rationale recorded in the header: a fresh MiSTer `.CFG` is all zeroes, so the .mra default is what boots; Fuuki's hand table dropped four `PORT_DIPUNUSED` bits in gogomile (default FF,1D instead of FF,FF) — `extract_dips.py:29-31`.

MAME `-listxml` is used only by the checker: the Seta core's `scripts/check_dips.py:1-20` compares each .mra against `<dipswitch>` masks/values/defaults and the `<control>` button count, reading the .mra the way `Main_MiSTer support/arcade/mra_loader.cpp` does.

### 3b. build_mra.py -> `<switches>`

Rules the emitters enforce (Seta `dip_xml`, `build_mra.py:639-686`):
- `ids` are indexed by the value assembled from the listed bits LSB-first: `for idx in range(1<<n): value |= 1<<bp ...; ids.append(settings.get(value,"-"))` (:666-672).
- `bits="first,last"` is a RANGE (mra_loader sscanf "%d,%d"); non-contiguous masks are rejected (:677-681, cites "issue #6").
- Undeclared bits default to 1 (pull-ups, all ports IP_ACTIVE_LOW): `default = 0xFF` then `(default & ~mask) | (dflt & mask)` (:654-661).
- Labels/names may not contain a comma (:673-675); Seta and KonamiGX also truncate to the OSD's 28 columns (`osd_fit` :605-636, KonamiGX `OSD_COLS = 28` :75 and check at :284-285).
- MAME "Unknown" switches go in as XML comments (Seta :1065-1072); Fuuki comments out "Flip Screen" (:620).

Byte layout per core (what byte i of `default=` means):
- KonamiGX: byte 0 = SW1 = SYSTEM_DSW bits 31:24 (0xd5a000), byte 1 = SW2 = bits 23:16 (`build_mra.py:249-253`, `KonamiGX.sv:222-228`). Example `releases/Sexy Parodius (ver JAA).mra:38-41`: `<switches default="FE,FF">`.
- MS32: 4 bytes, low byte first = the 32-bit word at 0xFCC00010 (`build_mra.py:114-117`, `MS32.sv:220-224`). Example `releases/Best Bout Boxing (ver 1.3).mra:12-29`, `default="FD,FF,FF,FF" base="0"`.
- Seta: byte 0 = DSW high byte (SW1), byte 1 = SW2, byte 2 = DIP bits in the COINS port top nibble (`Seta.sv:288-295`, `Seta.sv:395-403`; `check_dips.py:12-14`). Example `releases/Blandia.mra:50-61`, `default="FF,FF,FF" base="0"`.
- Fuuki: bytes 0-1 = DSW word, bytes 2-3 = FG-3's second DSW at $890000 (`Fuuki.sv:371-381`). Example `releases/Asura Blade - Sword of Dynasty (Japan).mra:67-79`, `default="3F,FF,FF,FF"`.
- Psikyo: byte 0-1 = DSW upper half at $C00004, byte 2 = region jumper (`Psikyo.sv:436-454`; `dsw_in = {dip_sw[15:8], dip_sw[7:0], 8'hFF, dip_sw[23:16]}`). Example `releases/Gunbird (World).mra:66-80`, `default="FD,FF,0F"`, Region uses `values="0x7,0xb,0xd,0xe,0xf"`.

### 3c. Core latch (ioctl index 254)

Same shape everywhere; Seta form (`Seta.sv:290-295`):
```
reg [7:0] sw[8];
always @(posedge clk_sys)
	if (ioctl_wr && (ioctl_index == 16'd254) && !ioctl_addr[24:3])
		sw[ioctl_addr[2:0]] <= ioctl_dout;
wire [15:0] dsw_in = {sw[0], sw[1]};
```
KonamiGX pre-loads driver defaults with `initial begin sw[0] = 8'hfe; sw[1] = 8'hff; end` (:225) so the board has sane DIPs before the .mra arrives; the others reset to FF. Psikyo.sv:436-447 documents that DIPs come via `arcade_sw_send()` and are saved in `config/dips/<mra name>`, not the .CFG.

### 3d. Joystick / buttons

hps_io ports used: `joystick_0`, `joystick_1` (all), `joystick_2/3` + `joystick_l_analog_0/1` + `ps2_mouse` (Seta.sv:145-195), `ps2_key` (MS32, Seta, Fuuki, Psikyo).

Bit convention (documented in every core, e.g. `KonamiGX.sv:183-187`, `Seta.sv` `buttons_xml` docstring): bits 0 R, 1 L, 2 D, 3 U; bit 4+i = entry i of the .mra `<buttons names=...>` list; joystick_N is active-high, driver ports are active-low so every bit is inverted.

CONF_STR `J1` lines and the fixed positions they define:
- KonamiGX.sv:71-72 `"J1,Button 1,Button 2,Button 3,-,-,-,Start,Coin,Pause,Service;"`, `"jn,A,B,X,-,-,-,Start,Select,L,R;"` -> Start 10, Coin 11, Pause 12, Service 13.
- MS32.sv:114-115 `"J1,Button 1,Button 2,Button 3,Button 4,Button 5,Start,Coin,Pause,Service,Test;"` -> Start 9, Coin 10, Pause 11, Service 12, Test 13.
- Seta.sv:87/91 (`CONF_J1`, Downtown variant renames buttons 3/4 "Rotate Left/Right"): B1-B6 at 4-9, Start 10, Coin 11, Pause 12, Service 13; `"jn,A,B,Start,Select,R;"` :134.
- Fuuki.sv:137 `"J1,Button 1,Button 2,Button 3,Button 4,Start,Coin,Pause;"` -> Start 8, Coin 9, Pause 10.
- Psikyo.sv:159 `"J1,Button 1,Button 2,Button 3,Button 4,Button 5,Button 6,Start,Coin,Pause;"` -> Start 10, Coin 11, Pause 12.

The .mra `<buttons>` list must be padded so Start/Coin stay at the bits the .sv reads. Seta `build_mra.py:809-833 buttons_xml()` records the hardware failure that motivates this (a two-button game put Start on the COIN1 bit) and pads to six button slots: `names = have + ["-"]*(6-n) + ["Start","Coin","Pause","Service"]`. Fuuki pads to `BUTTON_SLOTS` so Pause is always bit 10 (:559-566). KonamiGX hardcodes `BUTTONS` (:70-71). Psikyo.sv:376-396 relies on unmapped "-" slots reading 0 -> inverted 1 so one wiring serves the 3- and 6-button boards.

Port assembly (file:line):
- KonamiGX.sv:189-199: `player()` builds an 8-bit active-low byte `{start,B3,B2,B1,down,up,right,left}`; `inputs = {player(j0), player(j1), 16'hffff}`; `coins` bits 0/1 = joystick_N[11]; `service` bit 3 = joystick_0[13]|joystick_1[13].
- MS32.sv:227-239: `inputs[31:0]` with test (bit 19, joystick[13]), service (18, [12]), coins (17:16, joystick[10] or PS/2 keys 5/6), P1/P2 bytes. Mahjong sets: PS/2 key matrix `mjk[19:0]` :246-265, `mod_byte[5]` selects.
- Seta.sv:298-321 `seta_port(j, layout)` with `input_layout` from `rtl/seta_board_cfg.sv`; `coins_in` :395-403 (COIN1 0, COIN2 1, SERVICE1 2, TILT 3 never asserted, sw[2][7:4] top nibble); `extra_in` :327-330 (daioh buttons 4-6); `p3_in/p4_in` :420-421 (wits only); Downtown `dt_port` and `coins_in` :350-361; rotary via `rotary_input` :368-374; gun aim from `joystick_l_analog_*`/mouse :333-346.
- Fuuki.sv:343-369: `p1p2_in` ($810000) and `system_in` ($800000) with `sysport_alt` choosing between the gogomile and pbancho/asura bit layouts; SERVICE1 has no joystick slot (`service1 = 1'b0` :358), service mode is DSW bit 0.
- Psikyo.sv:396-419: `p1p2_in[31:0]`; `p1p2_low` differs per board (gunbird/btlkroad fold coins/service/TILT/z80 NMI into the low half; sngkace has a separate COIN port). `coin_in` at :636 (`board_gunbird ? 2'b11 : {~joystick_1[11], ~joystick_0[11]}`).

Pause: joystick bit toggles a latch, ORed with the Debug page "Pause CPU" — Seta.sv:407-418 `pause_core = pause_toggle | status[82] | dbg_rd_en`; Fuuki.sv:385ff `pause_control`; Psikyo.sv:496 `pause_btn = joystick_0[12] | joystick_1[12]`; MS32.sv:425 bit 11. KonamiGX: Pause is declared in J1 (bit 12) but nothing in KonamiGX.sv reads it (grep `joystick_0[12]` finds nothing) — unimplemented.

Service/test/tilt: Seta and KonamiGX expose Service on bit 13 and TILT is never asserted; MS32 has both Service (12) and Test (13); Fuuki and Psikyo have no service button (service mode via DIP; Psikyo ties SERVICE1/SERVICE/TILT high, :406-409).

Coin: level from the joystick bit, no pulse shaping or coin counter in any of the five; MS32 additionally accepts PS/2 keys 5/6 (:238, :258).

### 3e. Checks

- the Seta core's `scripts/check_dips.py` — .mra vs MAME `-listxml` `<dipswitch>` (masks, values, defaults) and `<control buttons=>`; runs MAME from `mame_capture.MAME_DIR/MAME_EXE`.
- the Seta core's `scripts/check_inputs.py` — every digital MAME field on P1-P4/COINS/EXTRA vs the core's `seta_port`/`dt_port` tables transcribed into the script (`SETA_PORT`), using `scripts/mame/ports.lua` cached in `debug/ports/<set>.txt`.
- the Psikyo core's `scripts/validate_mra.py:87-97` — `<switches default>` byte count covers every `<dip bits>`.

## 4. Checklist for a new core

1. Copy `scripts/extract_dips.py` from KonamiGX (latest copy); set `MAME_SRC` default to the new driver; extend `DEF_STR`; run `--selftest`.
2. In `build_mra.py`, copy Seta's `dip_xml` (contiguity check, default 0xFF, LSB-first ids, no commas) and `osd_fit`/`OSD_COLS = 28`; define the byte order of `<switches default>` to match the register the CPU reads, and write that order as a comment at the latch in the .sv (as KonamiGX.sv:222-223 and Fuuki.sv:371-373 do).
3. In the .sv: `"DIP;"` in CONF_STR; index-254 latch (`!ioctl_addr[24:N]` guard); `initial` defaults if the board is reset before the .mra arrives.
4. `J1` line with padded "-" slots so Start/Coin/Pause/Service sit at fixed bits; `buttons_xml` in build_mra.py padding to the same layout; `jn` line for the default pad map; `V,v` + `BUILD_DATE.
5. **Add a Pause input** even though the board has none: a `Pause` slot in `J1` that toggles a
   latch suspending the main CPU (and every CPU that must stay in step with it), so the game
   freezes mid-play. OR in the OSD-open pause and hiscore's `pause_cpu`; feed the combined
   pause back to hiscore's `paused`. Pattern: `Seta.sv:407-418`, Fuuki `rtl/pause_control.sv`
   (`ext_pause`). Suspend by clock-enable, never by holding reset or gating a clock.
6. Assemble ports with inverted joystick bits and a per-board layout selector (mod byte / board cfg), TILT tied high.
7. Copy Seta `check_dips.py` and `check_inputs.py` + `mame/ports.lua`; adapt the transcribed port tables.
8. Copy Psikyo `validate_mra.py` into the deploy step.

## 5. Defaults, .cfg and testing

- **Default every DIP to the setting that boots into the game**, not whatever the ROM's switch
  block happens to be. A service-mode or test DIP left on strands the player in the service menu
  (Seta's Thunder & Lightning did exactly that).
- **Comment out DIPs MAME marks unknown** in the generated `.mra`, with the entry left in place so
  a user can re-enable it. Do it in the generator, not by hand.
- **A changed default does not reach a game that already has a `.cfg` on the device.** MiSTer
  stores the status word per set in `/media/fat/config/<setname>.CFG`; a stale one leaves a DIP in
  a state the new build never intended. Delete or rewrite it (`cfg.py`) when defaults change, and
  say so when a change does not appear.
- **Exit to the OSD main menu between tests.** Launching a set while it is already loaded reuses
  what is in the FPGA, so a DIP or ROM change can silently not be under test.
- **Test inputs on a d-pad-only gamepad as well as analogue sticks, and for both players.** A gun
  aiming path that worked on sticks was broken on d-pads, and a fix applied to player 1 only
  looked like a player-2 bug (Seta).

## 6. Where the cores disagree

- **`bits=` encoding.** Seta (:677-681) and KonamiGX (:280-281) treat `bits` as a first,last range and reject gaps. MS32 `switches_xml` emits the full bit list (`",".join(map(str,pos))`, :136), so a 3-bit switch is written `bits="10,11,12"` (`Best Bout Boxing (ver 1.3).mra:20-21`). If mra_loader reads only the first two numbers (as Seta's comment states, citing "issue #6"), MS32's Coin A/B switches are 2-bit in the OSD. Unverified here; no MS32 doc records it.
- **Button bit positions** differ per core (Start at 8, 9 or 10). Seta/KonamiGX/Psikyo share Start 10 / Coin 11 / Pause 12; MS32 and Fuuki do not.
- **DIP source**: three cores parse the driver source; Fuuki hand-types; Psikyo hand-writes the .mra. Seta is the only core with a MAME-backed checker.
- **Pause**: implemented in four cores; declared but unread in KonamiGX.
- **Coin via keyboard**: MS32 only.
- **Default before .mra**: KonamiGX pre-loads driver defaults; others use FF.
